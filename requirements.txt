# Core dependencies for Voice AI Support Assistant
# Install with: pip install -r requirements.txt

# --- Direct Dependencies ---
groq>=1.0.0
sounddevice>=0.5.0
scipy>=1.15.0
numpy>=2.0.0
pyttsx3>=2.90
python-dotenv>=1.0.0

# --- Platform-specific dependencies ---
# Windows-only requirements (Markers ensure they are skipped on Linux/Mac)
pypiwin32>=223; sys_platform == "win32"
pywin32>=306; sys_platform == "win32"
comtypes>=1.4.0; sys_platform == "win32"

# --- Common helper dependencies (Transitive but pinned for safety) ---
requests>=2.30.0
pydantic>=2.0.0
httpx>=0.25.0
annotated-types>=0.6.0
anyio>=4.0.0
certifi>=2024.0.0
charset-normalizer>=3.0.0
distro>=1.8.0
h11>=0.14.0
httpcore>=1.0.0
idna>=3.4
sniffio>=1.3.0
typing-extensions>=4.10.0
urllib3>=2.0.0
